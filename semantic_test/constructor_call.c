{
    int i = int(true, 1);
    float a = float(false);
    vec2 b = vec2(1, true);

    int c = int(1);
    float d = float(0.0);
    vec2 e = vec2(1.0, 2.0);

    int h = int(c);
    vec2 w = vec2(d, d);
    vec2 w = e;
}
